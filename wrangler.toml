# Cloudflare Pages deploys only this staged static output directory.
# Build command: bash scripts/build-pages.sh
name = "usullearning"
pages_build_output_dir = "./dist"
compatibility_date = "2026-05-21"

[build]
command = "bash scripts/build-pages.sh"
